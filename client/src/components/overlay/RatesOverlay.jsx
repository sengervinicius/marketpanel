/** RatesOverlay — placeholder; the full Rates deep view lands two commits from now. */
export default function RatesOverlay() {
  return <div className="ol-placeholder">RATES DEEP VIEW — coming online…</div>;
}

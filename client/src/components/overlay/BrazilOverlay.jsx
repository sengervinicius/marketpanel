/** BrazilOverlay — placeholder; the full Brazil deep view lands in the next commit. */
export default function BrazilOverlay() {
  return <div className="ol-placeholder">BRAZIL DEEP VIEW — coming online…</div>;
}

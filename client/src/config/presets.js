/**
 * presets.js
 * Legacy re-export — all workspace presets now live in templates.js.
 * This file exists only for backward compatibility.
 */
export { WORKSPACE_TEMPLATES as SCREEN_PRESETS, getTemplate as getPresetById, getTemplateList as getPresetList } from './templates.js';
